# Reviewed vendor resources

Vendor code is not committed during Wave 0. `tools/vendor-lock.json` pins the reviewed oxmysql release. Install it locally into `resources/[vendor]/oxmysql`; that directory is ignored so an unreviewed or modified copy cannot be committed accidentally.
