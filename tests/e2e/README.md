# End-to-end tests

The complete Wave 1 user-facing flow now exists. Browser scenarios for every interactive lifecycle
destination are covered by TypeScript/Vitest and the reproducible two-entry production build.
Automated browser rendering and real FXServer connection scenarios remain a future harness; the
manual operator matrix is documented in `docs/verification.md`.
