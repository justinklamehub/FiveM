# End-to-end tests

Playwright and FXServer smoke scenarios are intentionally deferred until a user-facing flow exists. The Wave 0 NUI shell is covered by TypeScript tests and a reproducible production build.
