# Integration tests

Wave 0 integration verification runs the complete migration set against a real MariaDB 11.4.10 instance in CI. Repository-level database behavior tests will be added with the first domain schema.
